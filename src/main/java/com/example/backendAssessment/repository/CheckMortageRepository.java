package com.example.backendAssessment.repository;

import com.example.backendAssessment.entity.CheckMortage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckMortageRepository extends JpaRepository<CheckMortage, Integer> {
    
   
}
